import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dj.interactive.platform',
  appName: 'DJ Control Panel',
  webDir: 'dist'
};

export default config;
